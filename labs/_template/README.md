# Lab NN — <Title>

**Objective:** one sentence — what skill this proves, not what steps to run.

**Timebox:** ~XX min. If you're past double that, stop and ask in the
course discussion instead of pushing through alone.

**RHA lab:** <link to the relevant DO188/RH294 module, or "N/A — GitHub-native lab">

**FOSS equivalent** (if this is an RHA-linked lab): everything you do in the
RHA cloud lab has a free, self-hostable equivalent — noted here so you can
redo this without an RHA seat later:
- <e.g. "Podman → Docker: identical commands, swap the binary name">

## Quickstart

```bash
# the one command (or short block) that gets you to a running state
# before any explanation — copy, paste, go
```

## Definition of done

- [ ] <concrete, checkable item>
- [ ] <concrete, checkable item>
- [ ] Autograder passes on your final push (check the Actions tab)

## Grading

<what the autograder actually checks, in plain language — see
`.github/workflows/autograde.yml` for the implementation>
